message = "";

padding = 15;

limit = 20;

// blinking cursor
cursor = "|";
delay = 30;
alarm[0] = delay;

// if accepting input
accepting = false;

enter_presses = 0;

// to fix space inserted when pressing play
space_fixed = false;